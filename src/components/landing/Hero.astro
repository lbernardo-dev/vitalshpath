---
import { Image } from "astro:assets";
import { type Lang } from "../../i18n/ui-locales";
import { getSiteCopy } from "../../content/site-copy";
import { getAppStoreUrlForLang, getDefaultAppStoreLinks } from "../../lib/app-store-links";
import { ShieldCheck, ChevronRight } from "lucide-react";
import AppStoreBadge from "../common/AppStoreBadge.astro";

import iphoneHome from "../../assets/images/devices/iphone_home.png";
import iphoneMeds from "../../assets/images/devices/iphone_meds.png";
import iphoneCalendar from "../../assets/images/devices/iphone_calendar.png";

interface Props {
    lang: Lang;
}

const { lang } = Astro.props;
const copy = getSiteCopy(lang);
const appStoreHref = getAppStoreUrlForLang(lang, getDefaultAppStoreLinks());

const headingPart1 = "Take Charge Of Your Health.";
const headingPart2 = "Focus On The Journey.";
---

<section class="relative min-h-screen flex flex-col items-center justify-start pt-32 md:pt-48 pb-20 overflow-hidden">
    <!-- Ambient Glows -->
    <div class="absolute top-1/4 left-1/4 w-[500px] h-[500px] bg-teal-primary/10 blur-[140px] rounded-full animate-float -z-10"></div>
    <div class="absolute top-1/3 right-1/4 w-[400px] h-[400px] bg-orange-accent/5 blur-[120px] rounded-full animate-float -z-10" style="animation-delay: -3s;"></div>

    <div class="container mx-auto max-w-5xl px-4 sm:px-6 lg:px-8 relative z-10 w-full flex flex-col items-center text-center">
        <!-- Badge -->
        <div class="inline-flex items-center gap-2 px-5 py-2.5 rounded-full glass-panel mb-8 border-white/40 shadow-sm hover:scale-105 transition-transform duration-300">
            <ShieldCheck className="w-5 h-5 text-teal-primary" />
            <span class="text-[11px] font-black uppercase tracking-[0.2em] text-teal-primary">
                {copy.home.heroBadge}
            </span>
        </div>

        <!-- Headline -->
        <h1 class="text-4xl sm:text-5xl md:text-6xl lg:text-[5.5rem] leading-[1.05] mb-8 font-black tracking-[-0.04em] text-slate-text dark:text-white text-balance">
            {headingPart1} <span class="text-teal-primary block">{headingPart2}</span>
        </h1>

        <!-- Subtitle -->
        <p class="text-lg sm:text-xl text-slate-muted mb-10 leading-relaxed max-w-2xl mx-auto font-medium">
            {copy.home.heroSubtitle}
        </p>

        <!-- CTAs -->
        <div class="flex flex-wrap gap-6 justify-center mb-12 w-full reveal">
            <a
                href={appStoreHref}
                class="hover:scale-105 transition-all duration-300 active:scale-95 group/btn relative"
                aria-label="Download on the App Store"
            >
                <div class="absolute -inset-1 bg-gradient-to-r from-teal-primary/40 to-orange-accent/40 rounded-2xl blur-lg opacity-0 group-hover/btn:opacity-50 transition duration-500"></div>
                <div class="relative glass-panel-dark px-1 py-1 rounded-2xl border border-white/20 shadow-2xl">
                    <AppStoreBadge className="h-[48px] md:h-[52px]" />
                </div>
            </a>
            
            <a href="#features" class="group flex items-center gap-2 text-slate-text dark:text-white font-bold text-sm tracking-tight px-6 py-3 rounded-2xl bg-white/10 hover:bg-white/20 border border-white/20 backdrop-blur-xl transition-all duration-300">
                {copy.home.secondaryCta || "See Features"}
                <ChevronRight className="w-4 h-4 group-hover:translate-x-1 transition-transform" />
            </a>
        </div>

        <!-- Medical Disclaimer -->
        <p class="text-[11px] font-medium text-slate-muted/50 max-w-md mx-auto italic mb-20">
            {copy.common.medicalDisclaimer}
        </p>

        <!-- Phone Mockups Container -->
        <div class="relative w-full max-w-[320px] md:max-w-[420px] mb-32 reveal reveal-scale">
            <!-- Aura background -->
            <div class="absolute inset-0 bg-gradient-to-t from-teal-primary/30 to-orange-accent/20 blur-[100px] -z-10 rounded-full opacity-50 scale-125"></div>
            
            <div class="relative z-20 flex justify-center items-end">
                <!-- Side Device 1 -->
                <div class="absolute left-[-25%] sm:left-[-35%] bottom-0 w-[60%] opacity-40 blur-[2px] animate-float z-10" style="animation-delay: -3s;">
                    <Image
                        src={iphoneMeds}
                        alt="VitalsPath Meds View"
                        class="w-full h-auto select-none pointer-events-none mockup-clean-blend grayscale-[0.2]"
                    />
                </div>

                <!-- Side Device 2 -->
                <div class="absolute right-[-25%] sm:right-[-35%] bottom-0 w-[60%] opacity-40 blur-[2px] animate-float z-10" style="animation-delay: -7s;">
                    <Image
                        src={iphoneCalendar}
                        alt="VitalsPath Calendar View"
                        class="w-full h-auto select-none pointer-events-none mockup-clean-blend grayscale-[0.2]"
                    />
                </div>

                <!-- Main Device -->
                <div class="relative z-30 w-full hover:scale-[1.02] transition-transform duration-700 animate-float">
                    <div class="absolute -inset-10 bg-teal-primary/5 blur-[60px] rounded-full pointer-events-none"></div>
                    <Image
                        src={iphoneHome}
                        alt="VitalsPath Main Interface"
                        class="w-full h-auto drop-shadow-[0_40px_80px_rgba(0,0,0,0.15)] select-none pointer-events-none mockup-clean-blend"
                        loading="eager"
                    />
                </div>

                <!-- Floating interactive pills (Desktop only for precision) -->
                <div class="hidden lg:block absolute -top-8 -right-16 glass-panel py-4 px-6 shadow-2xl animate-float-slow border-white/60">
                    <div class="flex flex-col items-start gap-1">
                        <span class="text-[9px] font-black text-teal-primary tracking-[0.2em] uppercase">Live Sync</span>
                        <span class="text-[12px] font-bold text-slate-text">Healthy Metrics</span>
                    </div>
                </div>
                
                <div class="hidden lg:block absolute top-[60%] -left-20 glass-panel py-4 px-6 shadow-2xl animate-float border-white/60" style="animation-delay: -5s">
                    <div class="flex flex-col items-start gap-1">
                        <span class="text-[9px] font-black text-orange-accent tracking-[0.2em] uppercase">Private</span>
                        <span class="text-[12px] font-bold text-slate-text">Secure Cloud</span>
                    </div>
                </div>
            </div>
        </div>

        <!-- Footer Medical Note -->
        <div class="max-w-sm reveal">
            <p class="text-[10px] text-slate-muted/60 uppercase tracking-widest font-black leading-relaxed">
                * VitalsPath is not a medical device. Always consult with a healthcare professional for clinical decisions.
            </p>
        </div>
    </div>
</section>
