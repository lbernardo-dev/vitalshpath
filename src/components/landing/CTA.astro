---
import { ArrowRight, Mail } from "lucide-react";
---

<section id="waitlist" class="py-24 relative overflow-hidden">
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10 text-center">
    
    <div class="glass-card p-12 md:p-16 border-primary/20 bg-gradient-to-b from-white/80 to-primary/5 dark:from-zinc-900/80 dark:to-primary/10">
        <h2 class="text-4xl md:text-5xl font-extrabold mb-6 text-gray-900 dark:text-white">
          No te pierdas el lanzamiento
        </h2>
        <p class="text-xl text-gray-500 mb-10 max-w-2xl mx-auto">
          Únete a la lista de espera y sé el primero en probar VitalsPath. Recibirás un descuento especial de lanzamiento.
        </p>
        
        <form 
            action="https://formspree.io/f/mgokjbbp" 
            method="POST"
            class="flex flex-col sm:flex-row gap-4 max-w-lg mx-auto"
        >
            <div class="flex-1 relative">
                <Mail className="absolute left-4 top-1/2 -translate-y-1/2 text-gray-400 w-5 h-5" />
                <input 
                    type="email" 
                    name="email"
                    placeholder="tu@email.com" 
                    required
                    class="w-full pl-12 pr-4 py-4 rounded-xl border border-gray-200 dark:border-gray-700 bg-white dark:bg-black/50 focus:ring-2 focus:ring-primary focus:border-transparent outline-none transition-all"
                />
            </div>
            <button type="submit" class="px-8 py-4 bg-primary text-white font-bold rounded-xl hover:shadow-lg hover:shadow-primary/30 transition-all flex items-center justify-center gap-2">
                Unirse
                <ArrowRight className="w-5 h-5" />
            </button>
        </form>
        
        <p class="text-xs text-gray-400 mt-6">
            Sin spam. Solo actualizaciones importantes.
        </p>
    </div>
    
  </div>
  
  <!-- Background decoration -->
  <div class="absolute bottom-0 left-0 w-full h-1/2 bg-gradient-to-t from-primary/10 to-transparent -z-10"></div>
</section>
