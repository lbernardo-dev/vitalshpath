---
import Layout from '../../layouts/Layout.astro';
import ThemeToggle from '../../components/ThemeToggle.astro';
import LanguageSelector from '../../components/LanguageSelector.astro';
import Footer from '../../components/Footer.astro';
import { supportedLangs, type Lang } from '../../i18n/ui-locales';
import { useTranslations } from '../../i18n';

export function getStaticPaths() {
    return supportedLangs.map((lang) => ({
        params: { lang },
    }));
}

const { lang } = Astro.params;
const currentLang = lang as Lang;
const t = useTranslations(currentLang);
---

<Layout lang={currentLang}>
    <main class="relative overflow-hidden min-h-screen">
        <div class="fixed inset-0 pointer-events-none overflow-hidden z-0">
            <div class="absolute w-[500px] h-[500px] bg-blue-400/20 dark:bg-blue-500/10 rounded-full blur-[100px] top-[-5%] left-[-5%]"></div>
            <div class="absolute w-[400px] h-[400px] bg-purple-400/15 dark:bg-purple-500/10 rounded-full blur-[100px] top-[40%] right-[-5%]"></div>
        </div>

        <nav class="fixed top-0 left-0 right-0 z-50 bg-white/80 dark:bg-gray-900/80 backdrop-blur-xl border-b border-gray-200 dark:border-gray-800">
            <div class="container mx-auto px-6 py-4">
                <div class="flex items-center justify-between">
                    <a href={`/${currentLang}/`} class="flex items-center gap-2">
                        <img src="/assets/images/Icon.png" alt="VitalsPath" class="w-8 h-8 rounded-xl" />
                        <span class="text-lg font-bold text-gray-900 dark:text-white">VitalsPath</span>
                    </a>
                    
                    <div class="flex items-center gap-3">
                        <LanguageSelector lang={currentLang} />
                        <ThemeToggle />
                    </div>
                </div>
            </div>
        </nav>

        <section class="pt-32 pb-24 relative">
            <div class="container mx-auto px-6 max-w-4xl">
                <h1 class="text-4xl sm:text-5xl font-bold text-gray-900 dark:text-white mb-4">{t('legal.terms.title')}</h1>
                <p class="text-gray-600 dark:text-gray-400 mb-8">{t('legal.terms.subtitle')}</p>
                
                <div class="prose dark:prose-invert max-w-none text-gray-700 dark:text-gray-300 space-y-8">
                    
                    <section>
                        <h2 class="text-2xl font-bold text-gray-900 dark:text-white mb-4">{t('legal.terms.sections.general.title')}</h2>
                        <div class="space-y-4" set:html={t('legal.terms.sections.general.content')} />
                    </section>

                    <section>
                        <h2 class="text-2xl font-bold text-gray-900 dark:text-white mb-4">{t('legal.terms.sections.owner.title')}</h2>
                        <div class="space-y-4" set:html={t('legal.terms.sections.owner.content')} />
                    </section>

                    <section>
                        <h2 class="text-2xl font-bold text-gray-900 dark:text-white mb-4">{t('legal.terms.sections.purpose.title')}</h2>
                        <div class="space-y-4" set:html={t('legal.terms.sections.purpose.content')} />
                    </section>

                    <section>
                        <h2 class="text-2xl font-bold text-gray-900 dark:text-white mb-4">{t('legal.terms.sections.usage.title')}</h2>
                        <div class="space-y-4" set:html={t('legal.terms.sections.usage.content')} />
                    </section>

                    <section>
                        <h2 class="text-2xl font-bold text-gray-900 dark:text-white mb-4">{t('legal.terms.sections.healthkit.title')}</h2>
                        <div class="space-y-4" set:html={t('legal.terms.sections.healthkit.content')} />
                    </section>

                    <section>
                        <h2 class="text-2xl font-bold text-gray-900 dark:text-white mb-4">{t('legal.terms.sections.data.title')}</h2>
                        <div class="space-y-4" set:html={t('legal.terms.sections.data.content')} />
                    </section>

                    <section>
                        <h2 class="text-2xl font-bold text-gray-900 dark:text-white mb-4">{t('legal.terms.sections.accounts.title')}</h2>
                        <div class="space-y-4" set:html={t('legal.terms.sections.accounts.content')} />
                    </section>

                    <section>
                        <h2 class="text-2xl font-bold text-gray-900 dark:text-white mb-4">{t('legal.terms.sections.business.title')}</h2>
                        <div class="space-y-4" set:html={t('legal.terms.sections.business.content')} />
                    </section>

                    <section>
                        <h2 class="text-2xl font-bold text-gray-900 dark:text-white mb-4">{t('legal.terms.sections.export.title')}</h2>
                        <div class="space-y-4" set:html={t('legal.terms.sections.export.content')} />
                    </section>

                    <section>
                        <h2 class="text-2xl font-bold text-gray-900 dark:text-white mb-4">{t('legal.terms.sections.ip.title')}</h2>
                        <div class="space-y-4" set:html={t('legal.terms.sections.ip.content')} />
                    </section>

                    <section>
                        <h2 class="text-2xl font-bold text-gray-900 dark:text-white mb-4">{t('legal.terms.sections.liability.title')}</h2>
                        <div class="space-y-4" set:html={t('legal.terms.sections.liability.content')} />
                    </section>

                    <section>
                        <h2 class="text-2xl font-bold text-gray-900 dark:text-white mb-4">{t('legal.terms.sections.appstore.title')}</h2>
                        <div class="space-y-4" set:html={t('legal.terms.sections.appstore.content')} />
                    </section>

                    <section>
                        <h2 class="text-2xl font-bold text-gray-900 dark:text-white mb-4">{t('legal.terms.sections.thirdparty.title')}</h2>
                        <div class="space-y-4" set:html={t('legal.terms.sections.thirdparty.content')} />
                    </section>

                    <section>
                        <h2 class="text-2xl font-bold text-gray-900 dark:text-white mb-4">{t('legal.terms.sections.modifications.title')}</h2>
                        <div class="space-y-4" set:html={t('legal.terms.sections.modifications.content')} />
                    </section>

                    <section>
                        <h2 class="text-2xl font-bold text-gray-900 dark:text-white mb-4">{t('legal.terms.sections.termination.title')}</h2>
                        <div class="space-y-4" set:html={t('legal.terms.sections.termination.content')} />
                    </section>

                    <section>
                        <h2 class="text-2xl font-bold text-gray-900 dark:text-white mb-4">{t('legal.terms.sections.law.title')}</h2>
                        <div class="space-y-4" set:html={t('legal.terms.sections.law.content')} />
                    </section>

                    <section>
                        <h2 class="text-2xl font-bold text-gray-900 dark:text-white mb-4">{t('legal.terms.sections.contact.title')}</h2>
                        <div class="space-y-4" set:html={t('legal.terms.sections.contact.content')} />
                    </section>

                </div>
            </div>
        </section>

        <Footer lang={currentLang} />
    </main>
</Layout>
